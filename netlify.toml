[build]
  command = "npm run build || echo 'no build'"
  publish = "."

[dev]
  command = "netlify dev"
  port = 8888

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."tripletex"]
  node_bundler = "esbuild"
